# Firebase Cloud Functions local env (optional).
# Do NOT put FIREBASE_* or GOOGLE_CLOUD_PROJECT here — those prefixes are reserved.
# Use Secret Manager for production secrets:
#   npx -y firebase-tools@latest functions:secrets:set GEMINI_API_KEY --project sceneflow-f9529
#
# GEMINI_API_KEY is already defined as a secret in functions/src/index.ts.
